let params = prompt("Задание 5. Введите любые значения через пробел");
params = params.split(' ');
let arr = [];
alert(params.length + " - Количество элементов массива" );
for(var i=0; i<params.length; i++)
{
	alert(params[i]);
}
